<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book With Us!</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif; /* More modern font */
            background-color: #f9f9f9;
            margin: 0;
            padding: 0;
            color: #333;
        }
        h1 {
            background-color: #5d8aa8; /* Softer blue color */
            color: #fff;
            text-align: center;
            padding: 30px;
            margin: 0;
            font-size: 2.8em;
            font-family: 'Baskervville', serif; /* Elegant serif font */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-bottom: 5px solid #4a6e7c; /* Added border for a stylish touch */
        }
        form {
            max-width: 900px;
            margin: 40px auto;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
        }
        div {
            margin-bottom: 25px;
        }
        label {
            display: block;
            font-weight: 600;
            margin-bottom: 10px;
            color: #4a4a4a;
        }
        input[type="text"], input[type="email"], input[type="number"], input[type="date"], input[type="time"], textarea {
            width: calc(100% - 24px);
            padding: 14px;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-sizing: border-box;
            font-size: 1.1em;
            transition: border-color 0.3s, box-shadow 0.3s;
        }
        input[type="text"]:focus, input[type="email"]:focus, input[type="number"]:focus, input[type="date"]:focus, input[type="time"]:focus, textarea:focus {
            border-color: #5d8aa8;
            box-shadow: 0 0 8px rgba(93, 138, 168, 0.3);
            outline: none;
        }
        textarea {
            height: 150px;
            resize: vertical;
        }
        input[type="radio"] {
            margin-right: 8px;
        }
        button {
            background-color: #5d8aa8;
            border: none;
            color: white;
            padding: 15px 30px;
            border-radius: 10px;
            cursor: pointer;
            font-size: 1.1em;
            transition: background-color 0.3s, transform 0.3s;
        }
        button:hover {
            background-color: #4a6e7c;
            transform: translateY(-2px);
        }
        button:active {
            background-color: #3a5a65;
            transform: translateY(0);
        }
        .back-button {
            text-align: center;
            margin: 40px 0;
        }
        .back-button a {
            background-color: #5d8aa8;
            color: white;
            padding: 12px 24px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1em;
            transition: background-color 0.3s, transform 0.3s;
            display: inline-block;
        }
        .back-button a:hover {
            background-color: #4a6e7c;
            transform: translateY(-2px);
        }
        .back-button a:active {
            background-color: #3a5a65;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <h1>Booking Inquiry Form</h1>
    <div class="back-button">
        <a href="/">Back to Home</a>
    </div>
    <form action="/booking" method="POST"> 
        <div>
            <label for="username">Username:</label>
            <input type="text" id="username" name="username"/>
        </div>
        <div>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name"/>
        </div>
        <div>
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" />
        </div>
        <div>
            <label for="phoneNum">Phone Number:</label>
            <input type="text" id="phoneNum" name="phoneNum"/>
        </div>
        <div>
            <label for="eventOccasion">What is the occasion for your event?</label>
            <input type="text" id="eventOccasion" name="eventOccasion"/>
        </div>
        <div>
            <label>What are the services you are requesting?</label><br/>
            <input type="radio" id="backdrop-setup" name="services" value="Backdrop Setup">
            <label for="backdrop-setup">Backdrop Setup</label><br/>
            <input type="radio" id="proposal-setup" name="services" value="Proposal Setup">
            <label for="proposal-setup">Proposal Setup</label><br/>
            <input type="radio" id="luxury-picnic" name="services" value="Luxury Picnic Setup">
            <label for="luxury-picnic">Luxury Picnic Setup</label><br/>
            <input type="radio" id="event-decorator" name="services" value="Overall Event Decorator">
            <label for="event-decorator">Overall Event Decorator (backdrop setup, table decor, dessert table setup, lighting, accents, etc.)</label><br/>
        </div>
        <div>
            <label for="numPeople">Number of People:</label>
            <input type="number" id="numPeople" name="numPeople"/>
        </div>
        <div>
            <label for="eventDate">Date of Event:</label>
            <input type="date" id="eventDate" name="eventDate"/>
        </div>
        <div>
            <label for="startTime">Start Time:</label>
            <input type="time" id="startTime" name="startTime"/>
            <label for="endTime"> End Time:</label>
            <input type="time" id="endTime" name="endTime"/>
        </div>
        <div>
            <label for="location">What location will your event take place? (Type N/A if it hasn't been decided yet)</label>
            <input type="text" id="location" name="location"/>
        </div>
        <div>
            <label for="message">Anything else you'd like us to know?</label>
            <textarea id="message" name="message"></textarea>
        </div>
        <div>
            <button type="submit">Submit</button>
        </div>
    </form>
</body>
</html>




